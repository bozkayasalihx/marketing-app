import React from "react";
import { View, Text } from "../components/Themed";

interface SepetScreenProps {}

const SepetScreen: React.FC<SepetScreenProps> = ({}) => {
    return (
        <View>
            <Text>Sepet screen</Text>
        </View>
    );
};

export default SepetScreen;
